import cv2

def crop(image, top=70, bottom=25, left=0, right=0):
    return image
#    return image[top:-bottom, left:-right, :]

def resize(image, new_x=64, new_y=64):
    return image
#    return cv2.resize(image, (new_x, new_y))

